package com.app.incroyable.lockscreen.enums

import com.app.incroyable.lockscreen.R

enum class PatternLock(val layoutResId: Int) {
    Two(R.layout.layout_screen_main),
    Three(R.layout.layout_screen_pattern)
}